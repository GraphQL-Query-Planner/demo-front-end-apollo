import React, { Component } from 'react';

class Sidebar extends Component {
  render () {
    const style = {
      flex: '1'
    }

    return (
      <div style={style}>
        SIDEBAR
      </div>
    );
  }
}

export default Sidebar;
